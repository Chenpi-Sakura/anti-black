# AntiBlack System Requirements
# 黑灰产情报分析Agent系统依赖

# Web Framework
flask>=2.3.0
werkzeug>=2.3.0

# Database
pymongo>=4.5.0

# Kafka
aiokafka>=0.9.0

# LightRAG and its dependencies
# lightrag-hku @ file://./LightRAG  # Install from local clone during development

# LLM/Embedding
openai>=1.0.0
# NOTE: sentence-transformers removed - production uses Ollama API (bge-m3) via pipeline/classifier.py
# Kept only for local development/debugging
# sentence-transformers>=2.2.0
xgboost>=2.0.0
scikit-learn>=1.3.0
fasttext>=0.9.2
paddlepaddle>=3.0.0
paddleocr>=2.7.0
requests>=2.31.0

# Utilities
emoji>=2.14,<3.0
pyyaml>=6.0
python-dateutil>=2.8.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Development
python-dotenv>=1.0.0
jieba>=0.42.1

# Crawler helpers (Windows only)
# 用于 scripts/auto_click_cdp_dialog.py 自动点击 Chrome CDP 授权弹窗
pyautogui>=0.9.50; sys_platform == "win32"
Pillow>=10.0.0; sys_platform == "win32"
opencv-python>=4.8.0; sys_platform == "win32"
# mss: 多屏截屏, 修复 pyautogui 在 Windows 多屏下抓副屏的 bug
mss>=10.0.0; sys_platform == "win32"